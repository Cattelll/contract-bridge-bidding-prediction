pandas>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0
joblib>=1.3.0

# Deep Learning — MLP dan LSTM
# TensorFlow belum tersedia untuk Python 3.14 di environment ini.
# Fallback MLP berbasis scikit-learn tetap bisa dipakai tanpa TensorFlow.
# Jika ingin menjalankan LSTM, gunakan Python 3.12 atau lebih rendah lalu install TensorFlow secara manual.
keras>=2.13.0

# Double Dummy Solver — untuk DDS labeling (Step 4)
endplay>=0.4.7

# Jupyter Notebook
ipykernel>=6.0.0
notebook>=7.0.0
